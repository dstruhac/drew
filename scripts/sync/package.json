{
  "name": "drew-sync",
  "private": true,
  "type": "module",
  "description": "Scrapery a synchronizační skripty pro import rozpisu/výsledků — běží jen v GitHub Actions, nejsou součástí nasazené Next.js appky.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "playwright": "^1.48.0",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "vitest": "^2.1.4"
  }
}
